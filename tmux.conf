set -g mouse on
set -g history-limit 50000

# Reduce escape key delay (Claude says "important for Vim users")
set -sg escape-time 10

set -g default-terminal "screen-256color"

bind | split-window -h
bind - split-window -v

set -g set-clipboard on
setw -g mode-keys vi
